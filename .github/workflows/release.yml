name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ChatLog ${{ github.ref_name }}
          draft: true
          prerelease: false
          make_latest: false
          generate_release_notes: true

  publish-crates:
    name: Publish to crates.io
    needs: create-release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Publish to crates.io
        run: cargo publish --token ${{ secrets.CARGO_TOKEN }}

  build-release:
    name: Build & Upload
    needs: create-release
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: chatlog
            asset_name: chatlog-linux-x64
            target: x86_64-unknown-linux-musl
          - os: windows-latest
            artifact_name: chatlog.exe
            asset_name: chatlog-windows-x64
            target: x86_64-pc-windows-msvc

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install musl-tools (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: Build binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package binary
        shell: bash
        run: |
          cd target/${{ matrix.target }}/release
          if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            7z a ../../../${{ matrix.asset_name }}.zip ${{ matrix.artifact_name }}
          else
            tar -czf ../../../${{ matrix.asset_name }}.tar.gz ${{ matrix.artifact_name }}
          fi
          cd ../../..

      - name: Upload Release Asset
        uses: softprops/action-gh-release@v1
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          files: |
            ${{ matrix.asset_name }}.tar.gz
            ${{ matrix.asset_name }}.zip

  build-macos-universal:
    name: Build macOS universal binary
    needs: create-release
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Build universal binary (arm64 + x86_64)
        run: |
          cargo build --release --target aarch64-apple-darwin
          cargo build --release --target x86_64-apple-darwin
          lipo -create \
            target/aarch64-apple-darwin/release/chatlog \
            target/x86_64-apple-darwin/release/chatlog \
            -output chatlog
          chmod +x chatlog
          tar -czf chatlog-macos-universal.tar.gz chatlog

      - name: Upload Release Asset
        uses: softprops/action-gh-release@v1
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          files: chatlog-macos-universal.tar.gz

  update-formula:
    name: Update Homebrew Formula
    needs: [build-release, build-macos-universal]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Homebrew Tap
        uses: actions/checkout@v4
        with:
          repository: BeaversLab/homebrew-tap
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: homebrew-tap

      - name: Get Release Info
        id: get_release
        uses: actions/github-script@v7
        with:
          result-encoding: string
          script: |
            const release = await github.rest.repos.getReleaseByTag({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag: context.ref.replace('refs/tags/', '')
            });
            // Find the macOS artifact for Homebrew
            const asset = release.data.assets.find(a => a.name.includes('macos-universal'));
            if (!asset) {
               throw new Error("macOS universal asset not found");
            }
            return asset.browser_download_url;

      - name: Calculate SHA256
        id: shasum
        run: |
          # The asset is in a draft release, so we need a token to download it if we use API, 
          # but browser_download_url usually requires authentication for drafts too.
          # Actually, for drafts, browser_download_url might respond 404 to curl without auth.
          # But since we are inside Actions, we can try using the GITHUB_TOKEN header.
          
          curl -L -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" -H "Accept: application/octet-stream" -o chatlog.tar.gz ${{ steps.get_release.outputs.result }}
          echo "sha256=$(sha256sum chatlog.tar.gz | cut -d ' ' -f 1)" >> $GITHUB_OUTPUT

      - name: Update Formula
        run: |
          cd homebrew-tap
          mkdir -p Formula
          
          cat > Formula/chatlog.rb << EOF
          class Chatlog < Formula
            desc "Seamlessly sync, preserve, and version-control your AI coding conversations locally"
            homepage "https://github.com/BeaversLab/chatlog"
            url "${{ steps.get_release.outputs.result }}"
            sha256 "${{ steps.shasum.outputs.sha256 }}"
            version "${{ github.ref_name }}"
            license "Apache-2.0"

            def install
              bin.install "chatlog"
            end

            test do
              system "#{bin}/chatlog", "--version"
            end
          end
          EOF
          
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/chatlog.rb
          git commit -m "update: chatlog ${{ github.ref_name }}"
          git push
